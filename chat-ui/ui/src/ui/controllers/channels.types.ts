/**
 * Types for the channels controller.
 *
 * Re-exports the ChannelsState type so that the controller module
 * can import it from a co-located file.
 */

export type { ChannelsState } from "../views/channels.types.ts";
